<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Permissions — kept minimal to avoid Google Play Protect warnings.
        VIBRATE:        game feedback.
        READ_MEDIA_AUDIO: required for the in-game music picker (Android 13+).
        READ_EXTERNAL_STORAGE: legacy music access on Android 12 and below
                               (maxSdkVersion=32 so we don't request it on 13+).
        FOREGROUND_SERVICE + FOREGROUND_SERVICE_MEDIA_PLAYBACK:
                               required for MusicPlayerService (Android 9+ / 14+).
        WAKE_LOCK:      keep music playing while screen is off.
    -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
        Features — declare optional so the APK installs on devices without them.
    -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SaMacAoGiac"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="false"
        android:largeHeap="true"
        android:debuggable="false"
        android:appCategory="game"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        tools:targetApi="34">

        <!--
            v0.6: explicitly declare that we never read launcher content via
            content:// from outside, which reassures Play Protect.
        -->

        <activity
            android:name=".LoadingActivity"
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".GameActivity"
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:hardwareAccelerated="true"
            android:exported="false" />

        <!--
            v0.6: Kotlin music picker activity. Not exported — only launched
            from GameActivity.
        -->
        <activity
            android:name=".MusicPickerActivity"
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="false"
            android:theme="@style/Theme.SaMacAoGiac" />

        <!--
            v0.6: Foreground service for background music playback.
            foregroundServiceType="mediaPlayback" is required on Android 14+.
        -->
        <service
            android:name=".MusicPlayerService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
    </application>

</manifest>
